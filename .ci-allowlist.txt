# CI Leakage Scanner Allowlist — workflow
# Lines starting with # are comments.
# Entry = filename (skips entire file) or substring (skips matching lines).

# CPC setup documentation — legitimately references CPC paths, Volumes, env vars
per_machine_setup.md

# Skill documentation uses CPC data paths in examples
C:\CPC\
C:\\CPC\\
